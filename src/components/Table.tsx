// Re-export the Table component from the Table folder for backward compatibility
export { Table, default } from './Table/Table';
export type { TableColumn, PaginatedTableProps, SortConfig } from './Table/types';
